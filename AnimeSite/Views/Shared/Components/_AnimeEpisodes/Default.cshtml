@using EntityLayer.DTO
@model List<AnimesDetailEpsiode>


<div class="row">
    <div class="col-lg-12">
        <div class="anime__details__episodes">
            <div class="section-title">
                <h5>Bölümler</h5>
            </div>
            @foreach (var item in Model)
            {
                <a href="#">@item.Name</a>
            }
        </div>
    </div>
</div>
